[build]
    # command = "npm run build"
    command = "npm install -g sass && hugo --gc --minify --templateMetrics"
    publish = "public"

[build.environment]
    HUGO_VERSION = "0.118.2"
    HUGO_ENABLEGITINFO = "true"

# [[plugins]]
#     package = "netlify-plugin-hugo-cache-resources"
#     [plugins.inputs]
#         debug = false

# [[headers]]
#     for = "/portfolio/*"
#     [headers.values]
#         X-Robots-Tag = "noindex"